services:
  - type: web
    name: devportal-server
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DOMAIN
        value: tunnel.stylnode.in
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
